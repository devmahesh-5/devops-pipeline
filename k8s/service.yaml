apiVersion: v1
kind: Service
metadata:
  name: acquisitions
  labels:
    app: acquisitions
spec:
  type: NodePort
  selector:
    app: acquisitions
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
